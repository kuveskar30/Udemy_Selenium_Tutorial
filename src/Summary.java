
public class Summary {

	//1. Launching browser
	//a. using System.setproperty("webdriver.chrome.driver","driver_folder_path");
	//b. using WebDriverManager dependency
	
	//2. basic WebDriver methods
	//get("URL"), getTitle(), getCurrentUrl(), close(), quit()
	
	
	//3. window activities : navigation to url, 
	//d1.navigate.to("URL"), navigate.back(), navigate.forward()
	//d1.manage()window().maximize();
	//d1.manage().deleteAllCookies(), deleteCookieNamed("name")
	
	//4. locators
	//Id, className, name, cssSelector, xpath, linktext(a tag compulsory)
	
	//cssSelector: regex, parent-child, just child, :nth-child
	//tagName#id,tagname.className, tagName, tagname[attribute="value"]
	//tagname[attribute*="value"]
	
	//xpath : regex, parent-child, just child, xpath[number],
	//parent::, following-sibling::
	
	//tagname[@attribute="value"],tagName[contains(@atrribute,'value')]
	//tagname[text()='value'], //tagname[contains(text(),'value')]
	
	//absolute vs relative
	
	//uniquely identify by parent reference
	
	
}
